<!DOCTYPE html>
<html>
  <head>
    <title>Social Search</title>
    <style>
      body {
        font: 12px/1.5 'Lucida Grande', 'Lucida Sans Unicode', Verdana, Jamrul, sans-serif;
        text-align: center;
      }
      .container {
        text-align: left;
        margin: 0 auto;
      }
      table {
        margin-bottom: 1em;
      }
      tr td {
        padding: 2px;
        border-bottom: 1px solid #eee;
      }
      td.check {
        text-align: center;
      }
      /* search form */
      #search {
        margin-top: 20%;
      }
      #search h1 {
        font-size: 30px;
      }
      #search input, #search button {
        font-size: 20px;
        padding: 5px;
        border-radius: 3px;
      }
      #search input {
        border: 1px solid #ccc;
        outline: none;
      }
      #search button {
        font-weight: bold;
        background: #393;
        color: #fff;
        border: 0;
        cursor: pointer;
        text-transform: uppercase;
      }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>