!!!
%html
  %head
    %title Social Search
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body
    - unless flash[:error].nil?
      #error
        %p= flash[:error]
    = yield
    #footer
      %ul
        - unless @search.nil?
          %li#stats Your search for "#{@search.query}" returned #{@search.results.size} results..
        %li Social Search Beta (updated #{time_ago_in_words(Rails.application.class::FILE_VERSION)} ago).