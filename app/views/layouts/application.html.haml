!!!
%html
	%head
		%title Social Search
		= stylesheet_link_tag 'application', media: 'all'
		= javascript_include_tag 'application'
		= csrf_meta_tags
		%meta(property="og:title" content="Social Search")
		%meta(property="og:url" content="http://search.brogr.am/")
		%meta(property="og:image" content="#{image_path('logo@2x.png')}")
		%meta(property="og:description" content="This search engine generates results by searching Twitter for your query and forming clusters based on similarity of the returned Tweets.")
	%body{class: ('splash' if current_page?(root_path))}
		#header
			.container
				%h1#logo
					= link_to root_path do
						= responsive_image_tag 'logo.png'
				= display_flash.html_safe
		#content
			.container
				= yield
		#footer
			.container
				%ul
					%li (c) Fred Kelly 2013 (updated #{time_ago_in_words(Rails.application.class::FILE_VERSION)} ago)
					%li= modal_link 'What is this?', :about
		
		- if current_page?(controller: 'search', action: 'create')
			= render 'comment_form' # used in results.html.haml
		
		= render 'about' # info page